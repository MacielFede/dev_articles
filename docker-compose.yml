services:
  api:
    build:
      context: ./apps/api
    working_dir: /usr/src/app
    ports:
      - "4000:4000"
    volumes:
      - ./apps/api:/usr/src/app
    command: sh -c "npm install && nodemon server.js"
